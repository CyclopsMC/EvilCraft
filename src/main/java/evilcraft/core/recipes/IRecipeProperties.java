package evilcraft.core.recipes;

/**
 * Interface for additional properties (e.g. duration, energy usage) for recipes that can be registered with
 * {@link SuperRecipeRegistry}.
 *
 * @author immortaleeb
 */
public interface IRecipeProperties {
}
